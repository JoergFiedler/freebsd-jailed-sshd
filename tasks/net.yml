---
- name: Add network config
  lineinfile:
    dest: /etc/rc.conf
    line: "ifconfig_{{ jssh_jail_net_if }}_alias{{ jssh_jail_net_ip | ipaddr('int') }}=\"inet {{ jssh_jail_net_ip }} netmask 255.255.255.255\""
  register: add_ip

- name: Activate network config
  command: ifconfig {{ jssh_jail_net_if }} inet alias {{ jssh_jail_net_ip }} netmask 255.255.255.255
  when: add_ip.changed

