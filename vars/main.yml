---
# vars file for freebsd-jailed-sshd
ssh_user_master_passwd_entry: '{{ sshd_user }}:*:{{ sshd_user_uid }}:{{ sshd_user_gid }}::0:0:Remote (ssh) Management User:{{ sshd_user_home }}:{{ sshd_user_shell }}'
