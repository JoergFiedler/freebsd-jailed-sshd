# allow outgoing SSH from jump host to host's external ip
# which will be redirect to host's internal ip
pass out quick log on {{ jssh_jail_net_if }} proto tcp \
  from {{ jssh_jail_net_ip }} \
  to {{ jssh_host_net_ext_ip }} port {{ jssh_sshd_port }} \
  flags S/SA keep state

# Disable sshd to external interface
block log proto tcp \
  from any \
  to {{ jssh_host_net_ext_ip }} port {{ jssh_sshd_port }}

